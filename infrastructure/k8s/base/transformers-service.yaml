apiVersion: v1
kind: Service
metadata:
  name: transformers
  namespace: careersim
  labels:
    app.kubernetes.io/name: transformers
spec:
  selector:
    app.kubernetes.io/name: transformers
  ports:
    - name: http
      port: 8001
      targetPort: http
  type: ClusterIP


